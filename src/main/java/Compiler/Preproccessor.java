package Compiler;

public class Preproccessor {
    public static void proccess(Node entry) {

    }
/*
    public boolean proccess(Node entry, Node last, Node lastconst) {
        if(entry.getType().equals(Type.DECIMAL) && last.equals("+") && lastconst.getType().equals(Type.DECIMAL)) {
            last.update((Integer)entry.getValue() + (Integer)lastconst.getValue());
        }
    }
*/
}
